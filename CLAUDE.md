# money_manage

お金の管理アプリ（静的サイト: `index.html` / `style.css` / `app.js`）。データはブラウザの localStorage に保存し、Google ログイン中は `sync.js` で Firebase Firestore（プロジェクト moneymanage-2b6dc、`users/{uid}` に JSON 文字列）にも保存・同期する。
目的: クレジットは借金なので、引き落とし後〜給料日の「本当の貯金」と、その月に使っていい金額（マイナスにならない上限）を知ること。
給料日・締日・引き落とし日（固定）・次から3回分の引き落とし額（分割払い分）・固定費（支払済み・クレジット払いチェック付き。カード払いは締日→翌月引き落とし日に加算、今日より前の分は入力済み引き落とし額に含まれる前提）を入力し、今月から3ヶ月分（月ごと・左右の端タップとスワイプで切替、矢印なし）の残高と日ごとの使っていい金額（その日の利用分の引き落とし後〜次の給料日前の最小残高。それより先の引き落としは含めない）をカレンダーで表示する（背景色=少ない期間、文字色=マイナスかどうか）。
今の残高は入力した日（balanceDate）を基準に、その後の給料・引き落とし・固定費を反映して進める（日付が変わっても次に残高を変えるまで保持）。日付・月が変わると表示の3ヶ月も自動で進む。
アイコンは icon.svg から PNG を生成（デプロイ時にコピーするファイルは deploy.yml の cp に追加すること）。
テストはユーザー本人のみなので、デフォルト値はユーザーの実際の値（app.js の load()）。入力欄は色分け（残高=青、収入=緑、支出=赤）。最上部に「現時点の本当の貯金」（今が引き落とし後〜給料日ならその期間、そうでなければ次の引き落とし後〜次の給料日前日の最小残高）を大きく、その下に小さく「M/D締めまでにカードで使える額」（カレンダーの今日の「使える」。締日までの利用分の合計の上限）を表示（説明文なし）。その上に最後に入力を変えた日時（updatedAt。財務諸表の自動記録では変えない）を「最終更新」として表示。マイナスになるときは上部に「払えなくなる支払い」（日付・項目・不足額）を表示。今の残高は口座ごと（ゆうちょ・三菱）に入力して合計表示。給料・カード・固定費はそれぞれ口座を選び、払えるかは口座ごとに判定（奨学金=ゆうちょ）。カードの支払い金額確定日（12日）までに分割しないと払えない旨を表示。ゆうちょの不足は三菱から送金すれば払えるか（送金で三菱が後で足りなくなるか）も表示。おまけ機能として、ハンバーガーメニューから財務諸表（貸借対照表=今日時点の口座残高とカード未払金・純資産、損益計算書=毎月の給料−固定費、キャッシュフロー計算書=3ヶ月分の出入り）を表示できる（メインはカレンダー・入力画面）。財務諸表は毎回の計算で今月分を statements.current に記録し、月が終わると statements.history[月] に確定として残して、あとで月を選んで見返せる（C/F の「その他」＝予定以外の残高の変化）。ユーザーが指定した機能以外は入れないこと。

## デプロイ
- **作業ブランチは `claude/money-management-app-yzphwo`（デフォルトブランチ）。新しいセッションでもこのブランチで作業して直接 push すること**（別ブランチに push すると次のセッションに変更が引き継がれない）。
- GitHub Pages で公開。`.github/workflows/deploy.yml` が push 時に自動デプロイする。
- **修正したらすぐ commit & push してデプロイすること。**
- 画面右下にバージョン（`VERSION` のメジャー.マイナー + ワークフロー実行番号）とデプロイ時刻（JST）・コミットSHAが表示される。
  大きな変更のときは `VERSION` を上げる。
